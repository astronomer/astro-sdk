apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs
spec:
  capacity:
    storage: 70Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: nfs-server.benchmark.svc.cluster.local
    path: "/"
