kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nfs
  namespace: benchmark
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 70Gi
