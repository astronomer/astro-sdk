load_file:
  input_file:
    path: "s3://tmp9/homes_main.csv"
    conn_id: "aws_conn"
  output_table:
    conn_id: "redshift_conn"
    metadata:
      schema: "astro"
      database: "dev"
  normalize_config:
  is_dataframe: False
