name: "Pull Request Labeler"
on:
  pull_request:
    branches: [ 'main', 'release-**', 'astro-cli' ]
  # Run on PRs from forks
  pull_request_target:
    branches: [ 'main', 'astro-cli' ]

jobs:
  triage:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v4
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
        sync-labels: true
